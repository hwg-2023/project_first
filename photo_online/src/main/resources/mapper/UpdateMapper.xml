<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="photo_online.mapper.UpdateMapper">
    <update id="ban">
        UPDATE
        t_user
        set
        is_delete = #{isDelete}
        where
        id = #{id};
    </update>
</mapper>